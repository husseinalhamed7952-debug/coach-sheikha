# الدليل الشامل لتشغيل وإدارة منصة كوتش شيخة
### Coach Sheikha Platform — Complete Operation & Administration Guide

---

## 1. التشغيل المحلي والبناء (Installation & Build)

### المتطلبات الأساسية
- تثبيت **Node.js** (الإصدار 18 أو أحدث).
- موجه الأوامر (PowerShell أو Terminal).

### أوامر التشغيل
```bash
# 1. تثبيت حزم الاعتماد
npm install

# 2. تشغيل بيئة التطوير المحلية
npm run dev

# 3. بناء نسخة الإنتاج النهائية
npm run build
```

بعد تشغيل `npm run dev`، افتحي المتصفح على الرابط: `http://localhost:5173`.

---

## 2. إعداد مشروع Supabase الجديد المستقل (Supabase Setup)

> [!CAUTION]
> **تنبيه أمني هام:** لا تقومي بتنفيذ هذه الأوامر على مشروع Supabase القديم أو المشترك. جهزي مشروعاً جديداً ومستقلاً تماماً في Supabase لتفادي التأثير على أي بيانات سابقة.

### خطوات تجهيز قاعدة البيانات:
1. أنشئي مشروعاً جديداً في [Supabase](https://supabase.com).
2. افتحي **SQL Editor** من القائمة الجانبية في لوحة Supabase.
3. قومي بتشغيل ملفات المايجريشن المنظمة داخل مجلد `supabase/migrations/` بالترتيب:
   - **`001_schema.sql`**: ينشئ الجداول التسعة (`profiles`, `site_settings`, `site_content`, `packages`, `recipes`, `certificates`, `testimonials`, `bank_accounts`, `subscriptions`).
   - **`002_indexes.sql`**: ينشئ الفهارس لتحسين سرعة البحث والفرز.
   - **`003_rls.sql`**: يفعّل حماية مستوى الصفوف (RLS) ويضبط صلاحيات الزوار (قراءة المنشور فقط وإرسال الاشتراك) وصلاحيات المدير الكاملة.
   - **`004_storage.sql`**: ينشئ سلات التخزين وسياسات الأمان.
   - **`005_realtime.sql`**: يفعّل ميزة الاستماع اللحظي للاشتراكات في لوحة التحكم.
4. بعد تنفيذ ملفات المايجريشن، شغّلي ملف **`supabase/seed.sql`** لزرع البيانات الافتراضية المعتمدة للموقع.

---

## 3. إعداد التخزين (Storage Buckets)

تأكدي من ظهور السلات التالية في قسم **Storage** داخل Supabase:
- **`recipes`**: سلة عامة (Public) لصور الوصفات الصحية.
- **`certificates`**: سلة عامة (Public) لصور الشهادات.
- **`site-images`**: سلة عامة (Public) لصور الموقع والـ Hero.
- **`payment-receipts`**: سلة **خاصة ومحمية (Private)** مخصصة لسندات التحويل البنكي، ولا تفتح إلا للمدير المصرح له عبر روابط موقّعة مؤقتة (Signed URLs).

---

## 4. إعداد حساب المدير (Admin Authentication)

1. من لوحة Supabase، توجهي إلى **Authentication** -> **Users**.
2. اضغطي على **Add user** وأدخلي البريد الإلكتروني الخاص بك وكلمة مرور قوية.
3. انسخي معرّف المستخدم (User UID).
4. افتحي **SQL Editor** وشغّلي الأمر التالي لمنح الحساب صلاحية الإدارة:
```sql
INSERT INTO profiles (id, full_name, role)
VALUES ('معرف_المستخدم_هنا', 'كوتش شيخة', 'admin')
ON CONFLICT (id) DO UPDATE SET role = 'admin';
```
5. الآن يمكنك تسجيل الدخول إلى لوحة التحكم من الرابط: `/admin/login`.

---

## 5. متغيرات البيئة (Environment Variables)

أنشئي ملفاً باسم `.env` في المجلد الرئيسي للمشروع، وضعي القيم المستخرجة من إعدادات مشروع Supabase الجديد (**Project Settings** -> **API**):

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-publishable-key
```

> [!WARNING]
> **تحذير أمني:** لا تضعي مفتاح `service_role` أو كلمة مرور قاعدة البيانات في ملف `.env` أو داخل كود الواجهة الأمامية React إطلاقاً.

---

## 6. دليل استخدام وإدارة لوحة التحكم

### إدارة الباقات (`/admin/packages`)
- **حالة التوفر (Available / Closed):** عند الضغط على زر "متوفر/مغلق"، يتم تحويل حالة الباقة. إذا كانت الباقة مغلقة يظهر في الموقع "الاشتراك مغلق حالياً" ويتعطل زر الاختيار.
- **حالة الظهور (Published / Draft):** لإظهار الباقة في الموقع أو إخفائها كمسودة.
- **مدة الباقة:** يمكن تحديد المدة ووحدتها (مثل 7 يوم، 14 يوم، 30 يوم).
- **الحذف الآمن:** عند حذف أي باقة، لن تتأثر الاشتراكات القديمة بفضل وجود حقول لقطة السعر واسم الباقة (`package_name_snapshot`, `package_price_snapshot`).

### إدارة الاشتراكات (`/admin/subscriptions`)
- **البحث والفلترة:** يمكن البحث بالاسم أو رقم الجوال، والفلترة حسب الحالة (جديد، قيد المراجعة، مقبول، مكتمل، مرفوض) أو الباقة أو حالة الصلاحية (نشط، منتهي، قادم قريباً).
- **حساب التواريخ تلقائياً:** عند إدخال تاريخ البداية والمدة، يُحسب تاريخ النهاية تلقائياً مع إمكانية تعديله يدوياً.
- **معاينة السند الآمن:** بالضغط على أيقونة العين، تُفتح نافذة تفاصيل الاشتراك وتتيح معاينة صورة السند أو فتح ملف PDF عبر Signed URL مشفر ومؤقت.
- **التحديث اللحظي (Realtime):** تتحدث لوحة التحكم فور إرسال أي عميلة لاشتراك جديد دون الحاجة لتحديث الصفحة يدوياً.

### إدارة الوصفات الصحية (`/admin/recipes`)
- إضافة وتعديل الوصفات عبر نافذة مخصصة.
- إدخال القيم الغذائية (السعرات، البروتين، الكربوهيدرات، الدهون).
- إدخال المكونات وخطوات التحضير (كل خطوة في سطر منفصل).
- إدخال الفوائد الصحية وملاحظات الكوتش. (إذا تُرك الحقل فارغاً، لن يظهر أي صندوق فارغ في صفحة تفاصيل الوصفة).

### إدارة محتوى الموقع (`/admin/content`)
- تعديل نصوص وصور قسم البداية (Hero)، أرقام الإحصائيات (Stats)، قصة الكوتش (About)، خطوات الرحلة (Journey)، وقسم الدعوة (CTA).
- تظهر التعديلات فوراً في الصفحة الرئيسية للزوار دون الحاجة لتعديل الكود المصدري.

### تغيير رقم واتساب وبيانات التواصل (`/admin/settings`)
- يتم تعديل رقم الواتساب المعتمد من لوحة التحكم، ويرتبط تلقائياً برسائل الاشتراكات المشفرة وبأزرار الفوتر والتواصل دون أي تثبيت يدوي في الكود (Hardcoded).
- يتم تعديل حساب انستغرام والبريد الإلكتروني وحقوق الملكية.

### الحسابات البنكية والشهادات والآراء
- إدارة الحسابات البنكية المعتمدة التي تظهر في صفحة الاشتراك مع زر النسخ السريع.
- إضافة وتعديل الشهادات المعتمدة وقصص نجاح المشتركات وإمكانية إخفائها أو إعادة ترتيبها.

---

## 7. ملخص المسارات (Application Routes)

### المسارات العامة:
- `/` : الصفحة الرئيسية.
- `/packages` : الباقات التدريبية وجدول المقارنة.
- `/recipes` : الوصفات الصحية المجانية وشريط البحث.
- `/recipes/:slug` : تفاصيل الوصفة والقيم الغذائية والمكونات.
- `/subscription` : نموذج الاشتراك ورفع السند ونسخ الحسابات البنكية.

### مسارات لوحة التحكم:
- `/admin/login` : تسجيل دخول المدير.
- `/admin` : لوحة المعلومات والإحصائيات وآخر الطلبات.
- `/admin/content` : إدارة محتوى الموقع.
- `/admin/packages` : إدارة الباقات وتوفرها.
- `/admin/recipes` : إدارة الوصفات الصحية.
- `/admin/certificates` : إدارة الشهادات والاعتمادات.
- `/admin/testimonials` : إدارة قصص النجاح.
- `/admin/subscriptions` : إدارة الاشتراكات والبحث وعارض السندات.
- `/admin/bank-accounts` : إدارة الحسابات البنكية.
- `/admin/settings` : إعدادات المنصة ورقم واتساب.
