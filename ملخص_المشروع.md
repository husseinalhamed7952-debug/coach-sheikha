# ملخص مشروع كوتش شيخة

## ما تم عمله

- إنشاء مشروع React + Vite باسم **Coach Sheikha**.
- بناء واجهة عربية باتجاه RTL وبهوية بصرية قريبة من الصور المرفقة.
- إنشاء صفحات:
  - الرئيسية.
  - الباقات.
  - الوصفات الصحية.
  - تفاصيل الوصفة.
  - الاشتراك ورفع سند الإيداع.
  - تسجيل دخول الإدارة.
  - لوحة التحكم.
- إضافة شعار كوتش شيخة إلى الواجهة.
- إنشاء نموذج اشتراك يحتوي على البيانات الظاهرة في التصميم: الاسم، الجوال، البريد الاختياري، العمر، الطول، الوزن، الباقة، الحسابات البنكية، سند الإيداع والملاحظات.
- إعداد طبقة أولية للربط مع Supabase في `src/lib/supabase.js`.
- إنشاء مخطط أولي لقاعدة البيانات وسياسات RLS في `supabase/schema.sql`.
- إنشاء ملف متغيرات البيئة `.env.example`.
- إنشاء دليل إعداد أولي في `PROJECT_GUIDE_AR.md`.
- التحقق من نجاح بناء المشروع باستخدام `npm run build`.

## تشغيل المشروع محليًا

افتحي Terminal داخل مجلد المشروع، ثم نفذي:

```bash
npm install
npm run dev
```

بعدها افتحي الرابط التالي في المتصفح:

```text
http://localhost:5173/
```

لإنشاء نسخة إنتاج:

```bash
npm run build
```

## ما تبقى لإكمال المشروع

### 1. إعداد Supabase

- إنشاء مشروع Supabase.
- تشغيل محتوى `supabase/schema.sql` من صفحة SQL Editor.
- إنشاء حساب المدير للكوتش من Authentication.
- إضافة صف للمستخدمة في جدول `profiles` مع الصلاحية `admin`.

### 2. إعداد متغيرات البيئة

انسخي `.env.example` باسم `.env` وأضيفي القيم التالية:

```env
VITE_SUPABASE_URL=رابط_مشروع_Supabase
VITE_SUPABASE_ANON_KEY=مفتاح_Anon
```

> تحذير: لا تضعي `Service Role Key` في الواجهة أو داخل ملف `.env` المستخدم في React.

### 3. تحديث جدول الاشتراكات

النموذج الحالي يحتوي على حقول إضافية مطابقة للتصميم، لذلك يجب أن يدعم جدول `subscriptions` الحقول التالية:

- `email`
- `height`
- `weight`

### 4. إعداد التخزين Storage

إنشاء Buckets التالية في Supabase:

- `payment-receipts`: خاصة بسندات الدفع ولا تكون عامة.
- `recipes`: صور الوصفات.
- `certificates`: صور الشهادات.
- `site-images`: صور الصفحة الرئيسية وبقية صور الموقع.

### 5. تفعيل إرسال الاشتراك الحقيقي

يلزم استكمال التالي:

- رفع سند الدفع إلى `payment-receipts`.
- حفظ بيانات الطلب في جدول `subscriptions`.
- عرض الطلبات في لوحة التحكم.
- عرض سند الدفع للمدير فقط عبر Signed URL.
- حفظ رقم WhatsApp من الإعدادات.
- فتح رسالة WhatsApp جاهزة للكوتش بعد نجاح التسجيل.

### 6. استكمال لوحة التحكم

- CRUD فعلي للباقات.
- CRUD فعلي للوصفات الصحية.
- CRUD فعلي للشهادات.
- إدارة الحسابات البنكية.
- إدارة صور ونصوص الصفحة الرئيسية.
- تغيير حالة الاشتراك: جديد، قيد المراجعة، مقبول، مكتمل، مرفوض.
- تفعيل Realtime لإظهار الطلبات الجديدة دون تحديث الصفحة.

### 7. الصور النهائية

استبدال الصور التجريبية بصور كوتش شيخة الأصلية عند توفرها، ومنها:

- صورة Hero.
- صور الشهادات الأصلية.
- صور قصص النجاح.
- صور الوصفات.
- صورة قسم الدعوة للاشتراك.

## الحالة الحالية

المشروع يعمل كواجهة معاينة محلية، لكن استقبال الاشتراكات وحفظها وإدارتها فعليًا يتطلب إعداد Supabase واستكمال البنود السابقة.
