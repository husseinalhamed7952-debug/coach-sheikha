# تقرير الفحص والتدقيق الشامل لمشروع كوتش شيخة (PROJECT AUDIT)

تم إعداد هذا التقرير بعد فحص ومراجعة دقيقة وشاملة لجميع ملفات المشروع الحالية:
- `PROJECT_GUIDE_AR.md`
- `ملخص_المشروع.md`
- `supabase/schema.sql`
- `src/App.jsx`
- `src/services/api.js`
- `src/lib/supabase.js`
- `src/styles.css`
- `src/data/fallback.js`
- `package.json`
- الأصول والصور المرفقة في مجلد `public/`

ومقارنتها بشكل مفصل مع وثيقة المتطلبات الفنية للمشروع.

---

## 1. الصفحات الموجودة حاليًا

يحتوي المشروع حاليًا على المسارات التالية المعرفة في `src/App.jsx`:

1. **الصفحة الرئيسية (`/` - `Home`)**:
   - تحتوي على: الهيدر، قسم البداية (Hero)، شريط الإحصائيات (Stats)، نبذة عن الكوتش (About)، الشهادات المعتمدة، خطوات البدء، قصص النجاح (Testimonials)، قسم الدعوة للاشتراك (CTA)، والفوتر.
2. **صفحة الباقات (`/packages` - `Packages`)**:
   - تعرض الباقات الأربع الأساسية والمخصصة مع جدول مقارنة الميزات وزر الانتقال للاشتراك.
3. **صفحة الوصفات الصحية (`/recipes` - `Recipes`)**:
   - تعرض شبكة من كروت الوصفات الصحية (صورة، عنوان، مقتطف، رابط التفاصيل).
4. **صفحة تفاصيل الوصفة (`/recipes/:id` - `RecipeDetails`)**:
   - تعرض صورة الوصفة، جدول القيم الغذائية (السعرات، البروتين، الكارب، الدهون)، المكونات، وطريقة التحضير.
5. **صفحة الاشتراك وحجز الباقة (`/subscription` - `Subscription`)**:
   - نموذج حجز يحتوي على بيانات العميلة، اختيار الباقة، بيانات الحسابات البنكية لنسخها، رفع سند التحويل، وحقل ملاحظات.
6. **صفحة تسجيل دخول الإدارة (`/admin/login` - `AdminLogin`)**:
   - نموذج تسجيل الدخول عبر البريد الإلكتروني وكلمة المرور بواسطة Supabase Auth.
7. **لوحة التحكم الرئيسية (`/admin` - `Overview` داخل `AdminShell`)**:
   - تعرض إحصائيات سريعة وجدولاً لآخر الاشتراكات المسجلة.
8. **صفحة إدارة الجداول (`/admin/:table` - `Manager`)**:
   - واجهة عامة لعرض وحذف السجلات للجداول (`subscriptions`, `packages`, `recipes`, `certificates`, `bank_accounts`, `settings`).
9. **صفحة الخطأ (`*` - `NotFound`)**:
   - صفحة 404 للروابط غير المعرفة.

---

## 2. الصفحات والواجهات الناقصة

1. **الواجهة المفقودة لمعاينة سند الدفع وتفاصيل الاشتراك للمدير (Subscription Detail & Receipt Viewer)**:
   - لا توجد نافذة منبثقة أو صفحة فرعية تتيح للمدير النقر على الاشتراك لرؤية صورة السند المرفوع عبر رابط موقّع آمن (Signed URL)، أو قراءة بيانات العمر، الهاتف، الملاحظات، والباقة وسعرها.
2. **نوافذ ونماذج الإضافة والتعديل الفعلية (CRUD Modals / Forms) في لوحة التحكم**:
   - **الباقات (`/admin/packages`)**: لا توجد واجهة لإدخال السعر، الميزات، الشارة (Badge)، الوصف، أو ترتيب العرض. الزر الحالي يعتمد فقط على دالة المتصفح `prompt()` لإدخال الاسم فقط!
   - **الوصفات (`/admin/recipes`)**: لا توجد واجهة لإدخال القيم الغذائية (السعرات والماكروز)، قائمة المكونات، خطوات التحضير، أو رفع صورة الوصفة.
   - **الشهادات (`/admin/certificates`)**: لا توجد واجهة لرفع صورة الشهادة أو إدخال الوصف والجهة المانحة والترتيب.
   - **الحسابات البنكية (`/admin/bank_accounts`)**: لا توجد واجهة لإدخال اسم البنك ورقم الحساب واسم صاحب الحساب.
   - **إعدادات الموقع (`/admin/settings`)**: لا توجد واجهة لتعديل رقم الواتساب، اسم الكوتش، حساب انستغرام، البريد، وحقوق النشر.
   - **إدارة محتوى الموقع (`/admin/content` أو `site_content`)**: مفقودة تمامًا من القائمة الجانبية للوحة التحكم، ولا يوجد أي محرر لنصوص الـ Hero، قصة الكوتش، أو خطوات البداية.
   - **إدارة قصص النجاح والآراء (`/admin/testimonials`)**: غير موجودة إطلاقًا في لوحة التحكم رغم وجود جدول لها في قاعدة البيانات.
3. **واجهة النجاح وتأكيد الاشتراك (Subscription Success Modal / Fallback)**:
   - عند إرسال الاشتراك، يتم فقط عرض نص صغير أسفل النموذج ومحاولة فتح نافذة جديدة عبر `window.open`، والتي غالبًا ما تحظرها متصفحات الجوال (Popup Blockers). ينقص وجود نافذة منبثقة راقية تحتفي بالعميلة وتمنحها زرًا مباشرًا وواضحًا للانتقال إلى واتساب.

---

## 3. الوظائف الموجودة

- التنقل السلس بين الصفحات عبر `react-router-dom`.
- القائمة الجانبية المتجاوبة للهواتف (Mobile Drawer Menu).
- جلب الباقات والوصفات والحسابات البنكية من Supabase، مع الاعتماد على بيانات احتياطية (`fallback.js`) عند غياب الاتصال.
- نسخ رقم الحساب البنكي بنقرة زر (`navigator.clipboard.writeText`).
- تمرير معرّف الباقة تلقائيًا من صفحة الباقات إلى صفحة الاشتراك عبر الرابط (`/subscription?package=...`).
- رفع ملف الإيصال إلى Supabase Storage في bucket باسم `payment-receipts`.
- إدخال سجل الاشتراك في جدول `subscriptions`.
- تكوين رابط رسالة واتساب مشفر بنصوص الطلب وإطلاقه بعد الإرسال.
- تسجيل دخول الإدارة عبر `supabase.auth.signInWithPassword`.
- التحقق من وجود جلسة مسجلة في `AdminShell` لمنع الدخول غير المصرح به.
- حذف السجلات من الجداول العامة عبر الدالة `removeRow`.
- تسجيل الخروج من لوحة الإدارة (`supabase.auth.signOut`).

---

## 4. الوظائف الناقصة

1. **ديناميكية محتوى الصفحة الرئيسية بالكامل**:
   - نصوص الـ Hero، أرقام ونصوص الإحصائيات (Stats)، قصة الكوتش (About)، خطوات الرحلة (Steps)، وقصص النجاح (Testimonials) مكتوبة بشكل ثابت (Hardcoded) داخل ملف `App.jsx`. المتطلبات تنص صراحة على أن تكون جميعها قابلة للتعديل من لوحة التحكم دون لمس الكود.
2. **الشهادات المعتمدة في الصفحة الرئيسية**:
   - لا يتم جلب الشهادات من جدول `certificates` إطلاقًا! تم استبدالها بأسلوب CSS ثابت ومخادع (CSS Pseudo-elements).
3. **تغيير حالة الاشتراك في لوحة التحكم**:
   - المدير لا يستطيع تغيير حالة الطلب بين (`new`, `under_review`, `accepted`, `completed`, `rejected`).
4. **توليد روابط آمنة للسندات (Signed URLs)**:
   - لا توجد وظيفة تتيح للمدير عرض السند البنكي من الـ Bucket الخاص.
5. **البحث والفلترة والفرز في الاشتراكات**:
   - لا يمكن للمدير البحث باسم العميلة أو رقم الهاتف، ولا الفلترة حسب حالة الطلب.
6. **التحقق من حجم ونوع الملف في واجهة الاشتراك**:
   - لا يوجد فحص لحجم الملف في الواجهة (مثل حد أقصى 5MB أو 10MB) لمنع رفع ملفات ضخمة قد تستهلك الموارد أو تعلق الإرسال.
7. **منع التكرار (Idempotency Guard)**:
   - لا توجد حماية متقدمة تمنع إرسال نفس الطلب مرتين إذا نقرت العميلة عدة مرات بسرعة.
8. **معالجة حظر النوافذ المنبثقة للواتساب**:
   - عند إتمام الطلب، يتم الاعتماد على `setTimeout` مع `window.open` وهو ما تحظره متصفحات Safari وChrome على هواتف آيفون وأندرويد. يجب توفير زر تحويل مباشر تضغط عليه العميلة بنفسها.

---

## 5. مشاكل Supabase

1. **حقن بيانات الاتصال في الواجهة**:
   - ملف `.env` و `.env.example` يحتويان على عنوان مشروع Supabase ومفتاح الـ Anon الحقيقي. هذا طبيعي في تطبيقات React من جانب العميل، ولكنه يجعل من الضروري تفعيل RLS وسياسات الحماية على كافة الجداول والتخزين بلا استثناء (وهو غير مكتمل حاليًا).
2. **غياب التعامل مع انتهاء الجلسة (Session Lifecycle)**:
   - لا يوجد اشتراك في أحداث المصادقة `supabase.auth.onAuthStateChange` لمتابعة تجديد التوكن تلقائيًا أو تسجيل الخروج عند انتهاء الجلسة.
3. **فشل المعالجة عند انقطاع الاتصال**:
   - لا توجد طبقة إشعارات للمستخدم (Toasts/Notifications) لتوضيح أخطاء الاتصال أو تعذر الوصول إلى الخادم بطريقة واضحة وودية باللغة العربية.
4. **ثغرة تخطي التحقق في حال عدم توفر Supabase**:
   - في السطر 182 من `App.jsx`: `const [ready, setReady] = useState(!hasSupabase);`، في حال كان `hasSupabase = false` تصبح `ready = true` ولا يتم توجيه الزائر لصفحة الدخول، مما يسمح لأي شخص بفتح هيكل لوحة التحكم!

---

## 6. مشاكل Database (قاعدة البيانات والمخطط)

1. **عدم تطابق حقول جدول الاشتراكات (`subscriptions`) مع كود الواجهة**:
   - في ملف `src/services/api.js` (السطور 67-68)، يتم إرسال:
     ```javascript
     package_name_snapshot: selectedPackage ? selectedPackage.name : null,
     package_price_snapshot: selectedPackage ? selectedPackage.price : null,
     ```
   - بينما جدول `subscriptions` في `supabase/schema.sql` **لا يحتوي على هذين العمودين إطلاقًا!**
   - **النتيجة الكارثية**: عند تشغيل المشروع بربط Supabase حقيقي، سيفشل أي اشتراك جديد فورًا بخطأ Postgres: `column "package_name_snapshot" of relation "subscriptions" does not exist`.
2. **توافق معرّفات الباقات (UUID vs String)**:
   - حقل `package_id` في جدول `subscriptions` معرّف كـ `uuid references packages(id)`.
   - بيانات الـ Fallback للباقات تستخدم معرّفات نصية (مثل `basic-weekly`). إذا تم إرسال هذا المعرّف، سترفض قاعدة البيانات الطلب لخطأ في صياغة الـ UUID (`invalid input syntax for type uuid`).
3. **جدول `profiles` وحيد الحالة وخالٍ من Trigger الإنشاء التلقائي**:
   - تم تقييد الحقل `role text check(role='admin')`.
   - لا يوجد Trigger لإنشاء صف تلقائي في `profiles` عند تسجيل مستخدم جديد في Supabase Auth، مما يتطلب إدخاله يدويًا عبر SQL Editor، وإلا ستفشل كافة دوال التحقق `is_admin()`.
4. **غياب البيانات الأولية (Seed Data)**:
   - ملف `schema.sql` لا يزرع أي بيانات افتراضية لجدول `site_settings` أو `site_content`.
   - بالتالي عند تشغيل `supabase.from('site_settings').select('*').limit(1).single()` سيرجع الخطأ `PGRST116` (0 rows) ولن يتم العثور على رقم الواتساب.

---

## 7. مشاكل RLS (حماية مستوى الصفوف - Row Level Security)

1. **انعدام سياسات RLS على جدول `profiles`**:
   - تم تفعيل RLS على `profiles` بالسطر 12: `alter table profiles enable row level security;`.
   - **لكن لا توجد أي سياسة (Policy) مضافة له نهائيًا!**
   - النتيجة: عند تفعيل RLS بدون سياسات، يُمنع أي مستخدم (حتى بعد تسجيل دخوله كمدير) من قراءة بيانات ملفه الشخصي عبر الـ API.
2. **انعدام سياسات RLS على كائنات التخزين (`storage.objects`)**:
   - ملف `schema.sql` لا يحتوي على أي كود لسياسات الأمان الخاصة برفع وتحميل الملفات من Storage. (انظر قسم Storage أدناه).
3. **استدعاء `is_admin()` غير المحسن**:
   - السياسات الحالية: `using(published or is_admin())`.
   - بالنسبة للزائر غير المسجل، يتم استدعاء دالة `is_admin()` في كل صف يُقرأ، مما قد يسبب استعلامات فرعية مكررة داخل قاعدة البيانات. الأفضل فصل سياسة الزائر المستقلة: `using(published = true)`.

---

## 8. مشاكل Storage (التخزين)

1. **غياب سلات التخزين وسياستها في السكربت البرمجي**:
   - ملف `schema.sql` اكتفى بتعليق نصي: `-- أنشئي buckets: recipes, certificates, site-images (public عند الحاجة)، payment-receipts (خاص)`.
   - لم يتم إنشاء السلات عبر SQL، والأخطر: **لم تُنشأ سياسات `storage.objects`**.
2. **فشل رفع سندات الدفع للزوار**:
   - نظرًا لأن جدول `storage.objects` يخضع للـ RLS افتراضيًا في Supabase، فبدون سياسة صريحة تسمح للمستخدمين غير المسجلين (anon) بالـ `INSERT` داخل سلة `payment-receipts`، ستفشل عملية رفع السند فورًا بالخطأ: `new row violates row-level security policy for table "objects"`.
3. **غياب سياسة قراءة السندات للمدير فقط**:
   - سلة `payment-receipts` يجب أن تكون خاصة (`public = false`)، مع سياسة تسمح فقط للمدير (`is_admin()`) بقراءة الملفات أو إنشاء Signed URLs لها لمنع تسريب بيانات التحويلات البنكية الشخصية للعامة.
4. **انعدام سياسات الرفع لصور الوصفات والشهادات ومحتوى الموقع**:
   - لا توجد سياسات تسمح للمدير برفع صور جديدة للوصفات والشهادات ومحتوى الموقع، ولا سياسات تسمح للعامة باستعراضها.
5. **غياب دوال رفع الصور في الواجهة**:
   - لا يوجد في ملف `api.js` أي دالة لرفع صور الوصفات أو الشهادات، فقط رفع سند الدفع هو الموجود حاليًا.

---

## 9. مشاكل Authentication (المصادقة والصلاحيات)

1. **غياب التحقق من دور المستخدم (`role = admin`) في واجهة المتصفح**:
   - في `src/App.jsx`:
     ```javascript
     supabase.auth.getSession().then(({ data }) => {
       setReady(true);
       if (!data.session) nav('/admin/login');
     });
     ```
   - الواجهة تتحقق فقط من وجود جلسة مسجلة (`session`)، ولا تتحقق مما إذا كان هذا المستخدم يملك صلاحية `admin` في جدول `profiles`! إذا تم تسجيل أي مستخدم مستقبلاً، سيتمكن من فتح لوحة التحكم.
2. **رسائل الخطأ بلغة غير مفهومة للمستخدم**:
   - عند فشل تسجيل الدخول، يتم عرض رسالة الخطأ الخام باللغة الإنجليزية القادمة من Supabase مثل `Invalid login credentials` بدلاً من رسالة عربية واضحة ("البريد الإلكتروني أو كلمة المرور غير صحيحة").
3. **غياب استعادة كلمة المرور**:
   - لا يوجد خيار لاستعادة كلمة المرور للمديرة في حال نسيانها.

---

## 10. مشاكل Realtime (التحديث اللحظي)

1. **الـ Realtime معطل تمامًا في كود الواجهة**:
   - لا يوجد أي سطر كود واحد في كامل تطبيق React (`App.jsx` أو `api.js`) يستخدم `supabase.channel()` أو يستمع لتغييرات الجداول (`postgres_changes`).
   - هذا يخالف المتطلب الصريح الذي يطلب ظهور الاشتراكات الجديدة للكوتش في لوحة التحكم لحظيًا دون الحاجة لإعادة تحميل الصفحة (F5).
2. **نقص تفعيل الـ Realtime في قاعدة البيانات**:
   - لم يتم تفعيل الـ Publication عبر SQL لجدول الاشتراكات:
     `alter publication supabase_realtime add table subscriptions;` مكتفياً فقط بتعليق نصي في نهاية الملف.

---

## 11. مشاكل التصميم وواجهة المستخدم (CSS & UI)

1. **هيكل ملف الأنماط (`styles.css`)**:
   - الملف مضغوط في 6 أسطر عملاقة غير منسقة ويصعب صيانتها أو التعديل عليها بسلاسة.
2. **ألوان الهوية البصرية**:
   - المتغيرات الحالية: `--brown: #633200`. المتطلبات نصت صراحة على اللون البني الأساسي: `#522A07`.
   - يجب موازنة درجات البيج والرمال والذهبي الدافئ لتطابق تمامًا التصاميم المرجعية.
3. **حيلة CSS المخادعة للشهادات (CSS Pseudo-elements Hack)**:
   - في السطر 4 من `styles.css`:
     ```css
     .certificates img { display: none; }
     .certificates > div:before { content: 'Mezan Academy...'; }
     .certificates > div:after { content: 'إدراك...'; }
     ```
   - تم إخفاء صور الشهادات واستبدالها بنصوص ثابتة مولدة عبر CSS! هذا يمنع أي شهادة مضافة من لوحة التحكم من الظهور ويفسد الهيكل البصري للقسم.
4. **جداول لوحة التحكم البدائية**:
   - الجدول في لوحة الإدارة مصمم بأسلوب `repeat(4, 1fr)` بغض النظر عن الأعمدة المعروضة، ولا يتضمن شارات ملونة للحالات (Status Badges) مثل (جديد، قيد المراجعة، مقبول، مكتمل، مرفوض).
   - استخدام دوال المتصفح المنبثقة البدائية `prompt()` و `confirm()` كبديل عن النوافذ المنبثقة الراقية لإضافة وتعديل البيانات.
5. **الخطوط**:
   - الخط المستخدم حاليًا هو `Cairo` فقط، بينما المتطلبات أوصت بدمج أو دعم `IBM Plex Sans Arabic` للحصول على الطابع الفاخر والهوية المعتمدة.

---

## 12. المشاكل الأمنية (Security & Privacy)

1. **خطر كشف سندات التحويل البنكي للعامة**:
   - سندات الدفع تحوي أرقام حسابات وأسماء ومبالغ شخصية للعميلات. إذا تم رفعها بدون تفعيل سياسات Signed URL وسلة خاصة محمية بـ RLS، يمكن لأي شخص الوصول إليها أو العكس قد تفشل عملية الرفع بالكامل.
2. **انعدام تنظيف امتدادات وأسماء الملفات المرفوعة**:
   - في `api.js`: يتم أخذ الامتداد بـ `file.name.split('.').pop()` بدون تنظيف من الرموز الخاصة أو التأكد من مطابقة الـ MIME Type الفعلي للملف.
3. **غياب فحص الصلاحيات الإدارية من جهة العميل والخادم**:
   - كما لوحظ في سياسات RLS لجدول `profiles` وغياب التحقق الصارم من دور `role` في مكون `AdminShell`.
4. **مفاتيح البيئة المرفوعة**:
   - تأكيد التحقق من عدم وجود `service_role` key في الواجهة أو مستودع الكود. (الحالي هو anon key وهو سليم، لكن يجب تحصين RLS تمامًا لضمان عدم إساءة استخدامه).

---

## 13. الملفات التي تحتاج إلى تعديل

| الملف | نوع التعديل المطلوب |
|---|---|
| `supabase/schema.sql` | إضافة أعمدة سناب شات الباقة لجدول الاشتراكات، إضافة سياسات RLS لجدول `profiles` وكائنات `storage.objects`، وتفعيل الـ publication لـ Realtime، وإضافة البيانات الأولية لـ `site_settings` و `site_content`. |
| `src/lib/supabase.js` | تحسين إعداد العميل، تصدير دوال مساعدة لإنشاء الروابط الموقعة (Signed URLs)، وفحص الصلاحيات. |
| `src/services/api.js` | تصحيح كائن إرسال الاشتراك، إضافة دوال الإدارة الشاملة (CRUD) للباقات والوصفات والشهادات والحسابات البنكية والإعدادات ومحتوى الموقع، ودعم رفع الصور، ودعم Realtime. |
| `src/App.jsx` | تفكيك المكونات الأحادية، تحويل كافة أقسام الرئيسية لتعتمد على البيانات الديناميكية، بناء نوافذ ونماذج الإدارة المتكاملة مع معاينة السندات وتغيير الحالات، وإضافة التنبيه اللحظي للاشتراكات، وتحسين تجربة التحويل للواتساب. |
| `src/styles.css` | إعادة تنسيق الملف وفك الضغط، ضبط ألوان الهوية البصرية (`#522A07`)، إزالة اختراق إخفاء صور الشهادات، وإضافة أنماط النوافذ المنبثقة، وشارات الحالات، ومحررات البيانات. |
| `index.html` | استدعاء خط `IBM Plex Sans Arabic` إلى جانب `Cairo` وتحديث بيانات الميتا. |
| `PROJECT_GUIDE_AR.md` | تحديث دليل التشغيل وإعداد Supabase ليشمل الأوامر الدقيقة لإنشاء السلات وسياسات الأمان ودليل لوحة التحكم الكامل. |
| `ملخص_المشروع.md` | تحديث ملخص الإنجاز والوضع النهائي للمشروع. |

---

## 14. خطة تنفيذ كاملة مرتبة (Phased Implementation Plan)

### المرحلة 1: معالجة وتحديث قاعدة البيانات وتخزين Supabase (`supabase/schema.sql`)
1. تعديل جدول `subscriptions` بإضافة:
   - `package_name_snapshot text`
   - `package_price_snapshot numeric`
2. إضافة سياسات RLS الكاملة لجدول `profiles`.
3. إضافة سكربت تفعيل RLS وسياسات التخزين لجدول `storage.objects`:
   - سماح للزوار (`anon`) برفع السندات فقط إلى `payment-receipts`.
   - قراءة السندات وتوليد روابطها محصورة فقط بالمدير (`is_admin()`).
   - سماح برفع وإدارة الصور العامة في (`recipes`, `certificates`, `site-images`) للمدير، مع إتاحة قراءتها للعامة.
4. تفعيل ميزة التحديث اللحظي لجدول الاشتراكات:
   `alter publication supabase_realtime add table subscriptions;`
5. إضافة بذور بيانات أولية نموذجية (Seed Data) لكافة الجداول لضمان تشغيل الموقع بكامل طاقته بمجرد تشغيل ملف الـ SQL.

### المرحلة 2: ترقية وتوسيع طبقة الخدمات (`src/lib/supabase.js` & `src/services/api.js`)
1. تحسين `src/lib/supabase.js` بإضافة دوال مساعدة لإنشاء Signed URLs للسندات وPublic URLs للصور.
2. تطوير `src/services/api.js` ليشمل:
   - جلب وتحديث إعدادات الموقع ومحتوى الصفحة الرئيسية (`site_content`, `site_settings`).
   - جلب وتحديث وإضافة وحذف الوصفات والشهادات والباقات وقصص النجاح والحسابات البنكية مع دعم رفع الصور.
   - دالة توليد Signed URL لسند الدفع (`getReceiptSignedUrl`).
   - دالة تغيير حالة الاشتراك (`updateSubscriptionStatus`).
   - دالة الاشتراك في التحديثات اللحظية (`subscribeToNewSubscriptions`).

### المرحلة 3: تنسيق وتحسين ملف الأنماط (`src/styles.css`)
1. فك ضغط الأنماط وترتيبها في كتل منظمة وقابلة للقراءة والصيانة.
2. ضبط كود الألوان الأساسي وفق الهوية (`#522A07`، البيج، والرمال الدافئة).
3. إزالة كود الـ Hack المخفي لصور الشهادات (`display: none` و `:before/:after`).
4. إضافة تنسيقات النوافذ المنبثقة (Modals)، وشارات حالات الطلبات الملونة (Status Badges)، وأزرار النسخ والمعاينة.

### المرحلة 4: ربط الصفحة الرئيسية بمحتوى Supabase الديناميكي
1. ربط قسم الـ Hero ليقرأ النصوص والصور من جدول `site_content`.
2. ربط قسم الإحصائيات (Stats) وقصة الكوتش (About) وخطوات البداية بالمحتوى الديناميكي.
3. ربط قسم الشهادات بالكامل بجدول `certificates` ليعرض صور وبيانات الشهادات الحقيقية.
4. ربط قسم آراء وقصص النجاح بجدول `testimonials`.
5. ضمان استمرار عمل بيانات الـ Fallback بسلاسة في حال تعذر الاتصال بالخادم.

### المرحلة 5: تحسين رحلة الاشتراك وحجز الباقة
1. فحص حجم ونوع ملف سند الإيداع قبل رفعه لمنع الأخطاء.
2. معالجة وحماية زر الإرسال من النقر المزدوج (Double-click / Idempotency).
3. بناء نافذة نجاح تفاعلية راقية تظهر فور اكتمال الحجز تحتوي على زر واضح ومباشر لفتح محادثة الواتساب مع الكوتش لمنع حظر النوافذ المنبثقة من قبل المتصفح.

### المرحلة 6: استكمال وتطوير لوحة التحكم الإدارية بالكامل (`/admin`)
1. **حماية لوحة التحكم**: التحقق من امتلاك المستخدم صلاحية `admin` الفعلية قبل إتاحة العرض.
2. **إدارة الاشتراكات**:
   - عرض الاشتراكات مع إمكانية الفلترة بالحالة والبحث بالاسم أو الهاتف.
   - نافذة تفاصيل شاملة للاشتراك تعرض بيانات العميلة، الباقة، السعر، الملاحظات.
   - زر لعرض أو تنزيل سند التحويل عبر رابط موقّع آمن (`Signed URL`).
   - قائمة منسدلة لتغيير حالة الطلب بشكل لحظي.
3. **إدارة الباقات**: نموذج متكامل لإضافة وتعديل الباقات (الاسم، السعر، الشارة، الميزات، الحالة).
4. **إدارة الوصفات**: نموذج متكامل لإضافة وتعديل الوصفة مع رفع صورتها وقيمها الغذائية ومكوناتها وخطواتها.
5. **إدارة الشهادات**: إمكانية رفع شهادات جديدة بصورها وتعديلها أو حذفها.
6. **إدارة الحسابات البنكية**: إضافة وتعديل الحسابات البنكية التي تظهر للعميلات.
7. **إعدادات الموقع ومحتوى الصفحات**: واجهة مرنة لتعديل رقم الواتساب، وبيانات التواصل، ونصوص وصور الصفحة الرئيسية.
8. **التحديث اللحظي (Realtime Notifications)**: ظهور إشعار منبثق (Toast) وتحديث قائمة الطلبات فور قيام عميلة جديدة بإرسال اشتراكها.

### المرحلة 7: الفحص والاختبار وضمان الجودة (QA & Build Testing)
1. تشغيل `npm run build` للتأكد من خلو المشروع تمامًا من أية أخطاء برمجية أو أخطاء بناء.
2. اختبار رحلة المستخدم (Client Flow) على أجهزة الجوال وشاشات سطح المكتب.
3. اختبار رحلة الإدارة (Admin Flow) وتسجيل الدخول والعمليات المختلفة.
4. فحص الأمان ومنع الوصول غير المصرح به للملفات والبيانات الحساسة.

### المرحلة 8: التوثيق النهائي والتسليم
1. تحديث `PROJECT_GUIDE_AR.md` بكل التفاصيل الدقيقة لإرشاد الكوتش أو المطور لكيفية إدارة وتشغيل المنصة.
2. تحديث `ملخص_المشروع.md`.
